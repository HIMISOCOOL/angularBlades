import { BladeHostDirective } from './blade-host.directive';

describe('BladeHostDirective', () => {
  it('should create an instance', () => {
    const directive = new BladeHostDirective();
    expect(directive).toBeTruthy();
  });
});
